-- =========================
-- 1. CREATE TABLES
-- =========================

-- PROFILES (USERS)
create table profiles (
  id uuid primary key,
  full_name text,
  email text,
  role text default 'user',
  created_at timestamp default now()
);

-- PRODUCTS
create table products (
  id bigint generated by default as identity primary key,
  name text,
  price numeric,
  description text,
  seller_name text,
  seller_id uuid,
  status text default 'pending',
  created_at timestamp default now()
);

-- ORDERS
create table orders (
  id bigint generated by default as identity primary key,
  user_id uuid,
  total numeric,
  status text default 'pending',
  created_at timestamp default now()
);

-- ORDER ITEMS
create table order_items (
  id bigint generated by default as identity primary key,
  order_id bigint,
  product_id bigint,
  name text,
  price numeric
);

-- =========================
-- 2. ENABLE RLS
-- =========================

alter table profiles enable row level security;
alter table products enable row level security;
alter table orders enable row level security;
alter table order_items enable row level security;

-- =========================
-- 3. PROFILES POLICIES
-- =========================

create policy "Users read own profile"
on profiles
for select
to authenticated
using (auth.uid() = id);

create policy "Users insert profile"
on profiles
for insert
to authenticated
with check (auth.uid() = id);

create policy "Users update profile"
on profiles
for update
to authenticated
using (auth.uid() = id);

-- =========================
-- 4. PRODUCTS POLICIES
-- =========================

create policy "Anyone read products"
on products
for select
to public
using (true);

create policy "Users insert products"
on products
for insert
to authenticated
with check (auth.uid() = seller_id);

create policy "Update products"
on products
for update
to public
using (true);

create policy "Delete products"
on products
for delete
to public
using (true);

-- =========================
-- 5. ORDERS POLICIES
-- =========================

create policy "Users read own orders"
on orders
for select
to authenticated
using (auth.uid() = user_id);

create policy "Users insert orders"
on orders
for insert
to authenticated
with check (auth.uid() = user_id);

-- =========================
-- 6. ORDER ITEMS POLICIES
-- =========================

create policy "Read order items"
on order_items
for select
to authenticated
using (true);

create policy "Insert order items"
on order_items
for insert
to authenticated
with check (true);

-- =========================
-- 7. SAMPLE DATA
-- =========================

-- USERS (replace UUIDs with real ones if needed)
insert into profiles (id, full_name, email, role)
values
('11111111-1111-1111-1111-111111111111', 'Admin User', 'admin@test.com', 'admin'),
('22222222-2222-2222-2222-222222222222', 'Thabo Mokoena', 'thabo@test.com', 'user'),
('33333333-3333-3333-3333-333333333333', 'Sarah Nkosi', 'sarah@test.com', 'user');

-- PRODUCTS
insert into products (name, price, description, seller_name, seller_id, status)
values
('iPhone 11', 4500, 'Good condition iPhone', 'Thabo Mokoena', '22222222-2222-2222-2222-222222222222', 'approved'),

('Gaming Laptop', 8500, 'High performance laptop', 'Sarah Nkosi', '33333333-3333-3333-3333-333333333333', 'approved'),

('Nike Sneakers', 750, 'Comfortable running shoes', 'Thabo Mokoena', '22222222-2222-2222-2222-222222222222', 'approved'),

('Smart TV 42"', 3200, 'HD Smart TV with apps', 'Sarah Nkosi', '33333333-3333-3333-3333-333333333333', 'pending');

-- ORDERS
insert into orders (id, user_id, total, status)
values
(1001, '22222222-2222-2222-2222-222222222222', 4500, 'completed'),

(1002, '33333333-3333-3333-3333-333333333333', 3200, 'pending');

-- ORDER ITEMS
insert into order_items (order_id, product_id, name, price)
values
(1001, 1, 'iPhone 11', 4500),

(1002, 4, 'Smart TV 42"', 3200);
